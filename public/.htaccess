RewriteEngine On

# Disable LiteSpeed page cache for all requests
RewriteRule .* - [E=Cache-Control:no-cache]

# Also try to disable LSCache directly
<IfModule mod_pagespeed.c>
  Pagespeed Off
</IfModule>

<IfModule lsapi_module>
  LSCacheEngine Off
</IfModule>
